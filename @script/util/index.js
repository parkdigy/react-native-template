const replaceText = require('./replaceText');
const replaceTextExt = require('./replaceTextExt');
const checkFileExists = require('./checkFileExists');
const checkDirExists = require('./checkDirExists');
const deleteDir = require('./deleteDir');
const parseEnv = require('./parseEnv');

module.exports = {
  replaceText,
  replaceTextExt,
  checkFileExists,
  checkDirExists,
  deleteDir,
  parseEnv,
}
