/********************************************************************************************************************
 * FAQ API
 * ******************************************************************************************************************/

import api from '@api';
import {FaqList} from '@const';

export default {
  // 목록
  list(data: Dict) {
    return api.get<FaqList>('faq', data);
  },
};
