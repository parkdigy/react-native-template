import {Switch} from 'react-native-paper';
import {type FormSwitchProps as Props} from './FormSwitch.types';

const FormSwitch = (props: Props) => {
  return <Switch {...props} />;
};

export default FormSwitch;
