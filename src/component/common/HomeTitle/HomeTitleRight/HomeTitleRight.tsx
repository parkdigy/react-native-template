import {type HomeTitleRightProps as Props} from './HomeTitleRight.types';

export const HomeTitleRight = ({}: Props) => {
  /********************************************************************************************************************
   * Render
   * ******************************************************************************************************************/

  return <Stack row center mr={px.s_7} />;
};

export default HomeTitleRight;
