import React from 'react';
import {Tab1HomeProps as Props} from './Tab1Home.types';

export const Tab1Home = ({}: Props) => {
  return (
    <ContainerScrollView>
      <HomeTitle>Tab 1</HomeTitle>
    </ContainerScrollView>
  );
};

export default Tab1Home;
