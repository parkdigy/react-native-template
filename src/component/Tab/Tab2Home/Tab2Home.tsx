import React from 'react';
import {Tab2HomeProps as Props} from './Tab2Home.types';

export const Tab2Home = ({}: Props) => {
  return (
    <ContainerScrollView>
      <HomeTitle inTopContainer>Tab 2</HomeTitle>
    </ContainerScrollView>
  );
};

export default Tab2Home;
